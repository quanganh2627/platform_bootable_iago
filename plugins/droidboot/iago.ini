[partition.fastboot]
type = boot
src = droidboot.img
description = "Fastboot mode"
flags = noauto
# default: zero the partition out. Replace with "image" and add to base:bootimages to actually install it.
mode = zero

[base]
partitions += fastboot
